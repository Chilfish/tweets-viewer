# 2026-08-09

## 流程规范化：对标 Float 建立文档体系

从 Float 项目借鉴完整流程与规范，落地到本仓库：

- **CLAUDE.md**：弃用 AGENTS.md（符号链接），改为真实普通文件，新增「强制规范」段落（commit 纪律 / 文档先行 / postmortem 预读 / 规格驱动 / 测试先行）
- **docs/requirements/**：新增 PRD、用户故事、术语表
- **docs/planning/**：新增架构决策记录（ADR，8 条）、行动计划
- **docs/engineering/**：新增代码规范、Git 工作流、部署清单
- **docs/postmortem/**：建立尸检报告机制（索引 + 模板）
- **docs/development-log/**：建立按天开发日志
- **docs/archive/**：归档机制说明

动机：本仓库此前文档是静态规格快照，缺动态工作流闭环（踩坑沉淀 / 开发日志 / 发布规范）。对标 Float 后，AI 与人类协作有统一纪律。

踩坑：CLAUDE.md 从符号链接迁普通文件时 index mode 残留 symlink，`git rm --cached` + `git add` 修正 → 沉淀 [postmortem 001](../postmortem/001-windows-symlink-mode-sticky.md)。

## Fukuoka 归档抓取启动

新增 `apps/scripts/src/fetchSearchFukuoka.ts` + `apps/scripts/src/fukuoka/`（fetch/members/simplify），关键词 `福岡公演` 分页抓取入库。区别于既有 `fetchSearch.ts`（单页游标），新脚本支持游标循环多页抓取。

## Apple 视角设计锐评与处置

对项目整体设计做了 Apple 产品 + 研发双视角锐评（见 `docs/planning/review-r2-action.md`）：

- **总评**：技术骨相好、产品灵魂缺。架构决策（URL 驱动/服务端分页/JSON 列）教科书级，但产品是功能堆叠。
- **产品问题**：首页硬编码示例用户 `240y_k`、"那年今日"（最 Apple 的功能）无仪式感、移动端导航三层皮、meta 自我矮化。
- **研发问题**：三种分页模式并存（media 违反 URL 驱动）、`use-paginated-data.ts` 死代码、`ins.tsx` 用 workaround 掩盖 hydration bug、重试用整页 reload、`react-tweet` fork 与自研渲染并存、web 测试薄弱。
- **处置**：P0（架构一致性 4 项）→ P1（产品表达 3 项）→ P2（技术债 3 项），见 action-plan T9。

## P0 架构一致性处置（review-r2）

- **P0-1** 删除 `lib/use-paginated-data.ts` 死代码（121 行无人引用）
- **P0-2** `media.tsx` 并入 URL 驱动分页：loader 读取 URL `page`，滚动追加/跳页替换由 URL 变化驱动，移除纯客户端 `fetchNextPage` 路径；`TweetNavigation` 走 URL 驱动模式
- **P0-3** `ins.tsx` 错误处理重构：404 = 无 IG 归档的业务空态（保留），非 404 错误显式传给组件渲染"加载失败"+ 重试按钮，不再伪装成空态
- **P0-4** 重试机制统一为 `useRevalidator().revalidate()`（tweets/search/memo/media），删除 `window.location.reload()` 整页刷新

验证：typecheck ✅ lint ✅ web 10 tests ✅ build ✅


## P1 产品表达（review-r2，前两项）

- **P1-1 首页重做**：删除 `home.tsx` 硬编码 CTA（`to="/tweets/240y_k"`）；hero/meta 定位改为「完整人生归档」，去掉"第三方查看器"自我矮化；`useUserStore` 新增持久化 `recentUserNames` + `pushRecentUser`（去重 / 上限 6），layout loader 解析 activeUser 时记录最近浏览；首页新增「继续浏览 / 最近浏览 / 归档用户」入口（`components/home/user-entry.tsx` + `use-archive-users.ts`）
- **P1-2 那年今日仪式感**：`last-years-today.tsx` 新增仪式感头部（大字日期「8月9日」+ 回忆总数取自 `meta.total`）；推文按年分组展示（`lib/group-tweets-by-year.ts` 纯函数，连续段分组保序 + 年分隔线，不重排时间线）；首页「那年今日」仪式卡片入口（`components/home/memo-entry.tsx`，目标用户 = activeUser ?? recentUsers[0] ?? users[0]）
- 未做：P1-3 移动端导航职责重整（留待下轮）

验证：lint ✅ typecheck ✅ web 19 tests ✅ server 11 ✅ db 9 ✅ shared 20 ✅ build ✅

### UI 反馈处置（memo 页 + 顺带清扫）

- **memo 页仪式头部精简**：去掉与 ProfileHeader 重复的 `@name` 与双语 overline「那年今日 · On This Day」，收敛为「大字日期 + 一行统计」（往年今日，共 N 条回忆）；首页 memo 卡同步统一文案
- **memo 工具栏**：`TweetsToolbarActions` 新增 `hideDateRange`，memo 页隐藏日期范围筛选（API 不解析 start/end，原为静默无效的伪筛选）
- **修复**：`tweets/search/memo` 三处 `tweetAuthorName` 回退用了组件作用域未定义的 `name`（浏览器静默解析为 `window.name` 空串）→ 改为 `params.name`；search 空态文案中文化


## 日期范围筛选与 Calendar 迁移收尾

- 服务端 `fix(server): accept date-only values for the tweet date range`
- `feat(web): add DateRangeFilter` 并入推文工具栏
- `refactor(web): migrate Calendar to @daypicker/react v10`

（收尾项，前面 commit 已落地）
