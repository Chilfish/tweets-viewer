{
  "name": "@tweets-viewer/server",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy:server": "wrangler deploy --minify src/index.ts"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@hono-rate-limiter/cloudflare": "^0.2.2",
    "@tweets-viewer/core": "workspace:*",
    "hono": "^4.6.12",
    "wrangler": "^3.92.0"
  }
}
